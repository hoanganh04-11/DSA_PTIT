#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
	stack<int> st;
	int t; cin >> t;
	while(t--){
		string s; cin >> s;
		if(s == "push"){
			int n; cin >> n;
			st.push(n);
		}
		else if(s == "pop"){
			if(!st.empty()) st.pop();
		}
		else if(s == "show"){
			while(!st.empty()){
				c
			}
		}
	}
}
